
Процедура ПередЗаписью(Отказ)
	
	Если РасширенноеРедактирование Тогда
		Наименование = СокрЛП(first_name) + " " + СокрЛП(last_name) + " " + СокрЛП(patronymic) + " (" + СокрЛП(email) + ")";
	КонецЕсли;
	
КонецПроцедуры
