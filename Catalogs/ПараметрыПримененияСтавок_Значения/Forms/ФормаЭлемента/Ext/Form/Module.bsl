
&НаКлиенте
Процедура ВладелецПриИзменении(Элемент)
	УправлениеВидимостью();
КонецПроцедуры

&НаКлиенте
Процедура УправлениеВидимостью()
	Элементы.Диапазон.Видимость = ВернутьЗнчРеквизита(Объект.Владелец, "Диапазон");
Конецпроцедуры

&НаСервереБезКонтекста
Функция ВернутьЗнчРеквизита(Реквизит, Значение);
	Возврат Реквизит[Значение];
КонецФункции

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	УправлениеВидимостью();
КонецПроцедуры

